# Pre-fix visual audit

Synthetic, public-safe reproduction for a proposed footer section-width issue (ISSUE_PENDING).

Microsoft Excel keeps the center footer "Generated by Example Reporting System" on one line. office2pdf wraps "System" onto a second line despite sufficient page width. The source oddFooter has no line break; generate_hf_content allocates each section one third of the available width through three 1fr grid columns.

Model vision findings: inspected the 150 DPI full GT/output pages, side-by-side image, full pixel diff, four matched shift crops, and full-scale footer pair. Both files have one page with the same body rows and values. Center footer wrapping is the new finding. Header is 1.60 pt above reference (tracked in #1731); three body anchors are 1 pt above reference (tracked in #1721). Text remains black, regular Arial with the same apparent size. Left/right footer content remains present. No rotated elements, fills, borders, decorations, emphasized runs, hairlines, or clipping are present in this synthetic sheet. The additional center-footer line is the only new visible defect in this pass. No runtime fix or post-fix pass is claimed.

The text-layer comparison reports equal normalized lengths but changed extraction order: the first wrapped center line precedes the left/footer-bottom row. Direct pdftotext inspection confirms every word remains present. Layout reports the footer as one GT line regrouped into two output lines, with no lost text.
