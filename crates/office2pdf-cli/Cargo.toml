[package]
name = "office2pdf-cli"
version = "0.7.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "CLI tool to convert DOCX, XLSX, and PPTX files to PDF"
readme = "../../README.md"
keywords = ["pdf", "docx", "xlsx", "pptx", "cli"]
categories = ["command-line-utilities"]

[[bin]]
name = "office2pdf"
path = "src/main.rs"

[features]
default = ["embedded-fonts"]
embedded-fonts = ["office2pdf/embedded-fonts"]
server = ["tiny_http"]

[dependencies]
office2pdf = { version = "0.7.0", path = "../office2pdf", default-features = false, features = ["pdf-ops"] }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
rayon = "1"
tiny_http = { version = "0.12", optional = true }

[dev-dependencies]
docx-rs = "0.4.22"
lopdf = "0.45"
