# Bug-fix evidence

Store visual regression evidence in an issue-numbered directory:

```text
assets/bugfixes/issue-<number>/before.png
assets/bugfixes/issue-<number>/after.png
```

Generate both images from the same input document, page, resolution, and renderer. These paths can be linked from GitHub issues and pull requests after the branch is pushed.
